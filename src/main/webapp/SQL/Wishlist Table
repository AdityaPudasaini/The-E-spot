CREATE TABLE wishlist (
    Wishlist_ID INT(11) PRIMARY KEY AUTO_INCREMENT,
    Member_ID INT(11),
    Created_At DATETIME,
    FOREIGN KEY (Member_ID) REFERENCES member(Member_ID)
);

INSERT INTO wishlist (Member_ID, Created_At) VALUES (?, NOW());

SELECT Wishlist_ID FROM wishlist WHERE Member_ID = ?;